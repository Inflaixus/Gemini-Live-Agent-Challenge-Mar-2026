steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -t
      - '$_IMAGE_URL'
      - -f
      - Live-Agent-New-Version/Dockerfile
      - .

images:
  - '$_IMAGE_URL'

options:
  machineType: 'E2_HIGHCPU_8'
